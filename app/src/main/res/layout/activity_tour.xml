<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context="org.shop2care.android.shop2care.app.ui.TourActivity">

    <ImageView
        android:id="@+id/logo_tour"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="@dimen/activity_vertical_padding"
        android:paddingTop="@dimen/activity_vertical_padding"
        android:contentDescription="@null"
        android:background="@color/white"
        android:layout_alignParentTop="true"/>

    <LinearLayout
        android:id="@+id/layout_logo_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:gravity="center"
        android:paddingBottom="@dimen/activity_vertical_padding"
        android:paddingTop="@dimen/activity_vertical_padding"
        android:background="@color/white">

        <Button
            android:id="@+id/tour_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingLeft="@dimen/activity_horizontal_margin"
            android:paddingRight="@dimen/activity_horizontal_margin"
            android:gravity="center" />
    </LinearLayout>

    <com.viewpagerindicator.CirclePageIndicator
        android:id="@+id/tour_pager_indicator"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp"
        android:layout_above="@id/layout_logo_button"
        app:fillColor="@color/fill_circle_indicator"
        app:radius="10dp"
        app:strokeColor="@color/stroke_circle_indicator"
        app:strokeWidth="1dp"/>


    <android.support.v4.view.ViewPager
        android:id="@+id/tour_pager"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/tour_pager_indicator"
        android:layout_below="@id/logo_tour"
        android:gravity="center"/>

</RelativeLayout>
